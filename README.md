# Secure-Secrets-Manager-Project
The repository which contains the files of the Secure Secrets Manager project.


###Workflow for first time setup:

1. install packages:
pip install -r requirements.txt

2.	Then run this file to generate encryption keys:
python generate_keys.py

3.	Then: run the app file:
python app.py
